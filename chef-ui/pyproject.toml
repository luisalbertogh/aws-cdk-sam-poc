[project]
name = "chef-ui"
version = "0.1.0"
description = "Chat UI for the Chef Assistant."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chainlit==2.11.0",
    "boto3>=1.34.0",
    "python-dotenv>=1.0.1"
]
